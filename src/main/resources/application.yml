spring:
  security:
    user:
      name: admin
      password: admin
      roles: ADMIN
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
  session:
    jdbc:
      table-name: TEST_SESSION
